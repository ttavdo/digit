[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# React Router — all routes serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
